name: aws cred test

on:
  workflow_dispatch

jobs:
  foo:
    permissions:
      id-token: write
      contents: read

    runs-on: ubuntu-latest
    environment: prod
    steps:
      - uses: ./.github/actions/dep

